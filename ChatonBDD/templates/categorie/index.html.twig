{% extends 'base.html.twig' %}

{% block title %}Super Chaton{% endblock %}

{% block body %}

    <h1>Bienvenue sur Super ChatonBDD !</h1>
    <p>Choisissez une catégorie dans le menu ci-dessous</p>

    <table>
        <thead>
        <tr>
            <td>Nom</td>
            <td>Description</td>
        </tr>
        </thead>
        <body>
    {% for categorie in lesCategories %}
        <tr>
            <td>{{ categorie.nom }}<br></td>
            <td>{{ categorie.description }}<br></td>
            <td>
                <a href="{{  path("categorie_modifier",{"id":categorie.id} )  }}" 
                class="btn-floating btn-small waves-effect waves-light green">
                <i class="material-icons">create</i></a>
                <a href="{{  path("chaton",{"id_categorie":categorie.id} )  }}" 
                class="btn-floating btn-small waves-effect waves-light blue">
                <i class="material-icons">remove_red_eye</i></a>
                <a href="{{  path("categorie_verif",{"id":categorie.id} )  }}" 
                class="btn-floating btn-small waves-effect waves-light red"><i class="material-icons">close</i></a></td>
        </tr>
        
    {% endfor %}
    </body>
    </table>

{% endblock %}